// Well being journey for the current day

import 'package:flutter/material.dart';

class WellBeingJourneyToday extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('well being journey Today'),
    );
  }
}
